import styled from 'styled-components';
import { Media, MediaImage } from '@/shared/Media';

export const FirstMedia = styled(Media)`
	align-items: flex-start;
	margin-bottom: 0;
`;

export const FirstMediaImage = styled(MediaImage)`
	margin-top: -140px;
`;